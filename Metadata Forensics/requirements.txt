# ─────────────────────────────────────────────────────────────
# TrustNet — 03_metadata_forensics/requirements.txt
# Tested: Python 3.13 · Windows 11 · 64-bit
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# PDF metadata extraction
# PyMuPDF 1.25+ ships pre-built wheels for Python 3.13 on Windows —
# no C compiler needed. Do NOT pin below 1.25.0 on Python 3.13.
PyMuPDF==1.26.1

# fallback PDF reader for /Info dictionary
pypdf==4.3.1

# Office document metadata (DOCX, XLSX, PPTX)
python-docx==1.1.0
openpyxl==3.1.2
python-pptx==0.6.23

# Image metadata / EXIF
Pillow==11.1.0
piexif==1.1.3

# ExifTool Python wrapper
# NOTE: also install the ExifTool binary from https://exiftool.org/install.html
# and add it to your Windows PATH. The module degrades gracefully if absent.
pyexiftool==0.5.6

# File type detection (Windows note: use python-magic-bin, not python-magic)
# python-magic needs libmagic.dll which is not included on Windows.
# python-magic-bin bundles the DLL — drop-in replacement, same import name.
python-magic-bin==0.4.14

# Timezone handling for date comparison
python-dateutil==2.9.0

# Structured output / typing
pydantic==2.11.4

# Logging
loguru==0.7.3