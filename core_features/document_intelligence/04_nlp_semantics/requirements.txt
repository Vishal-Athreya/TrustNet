# ─────────────────────────────────────────────────────────────
# TrustNet — 04_nlp_semantics/requirements.txt
# Tested: Python 3.13 · Windows 11 · 64-bit
# Install: pip install -r requirements.txt
#
# After install, run these two commands:
#   python -m spacy download en_core_web_sm
#   (Only needed once — downloads the spaCy English NER model)
# ─────────────────────────────────────────────────────────────

# Core NLP / transformer models
# transformers 5.x = current stable, supports Python 3.13
transformers==4.53.3

# PyTorch — pick the right line for your machine and uncomment it.
# CPU-only (works on any laptop, no GPU required — slower but fine for dev):
torch==2.7.1

# If you have an NVIDIA GPU with CUDA 12.1, use this instead (comment out line above):
# pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu121

# Sentence-level semantic similarity
sentence-transformers==5.6.0

# OCR — extract text from scanned images and image-only PDFs
pytesseract==0.3.10
# NOTE: Tesseract binary required. Windows installer:
# https://github.com/UB-Mannheim/tesseract/wiki
# After installing, add Tesseract to PATH or set in code:
#   pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'

Pillow==11.1.0
opencv-python-headless==4.13.0.92

# PDF text extraction (native text layer, faster than OCR)
PyMuPDF==1.26.1

# Named Entity Recognition
# spaCy 3.8 has Python 3.13 wheels on Windows
spacy==3.8.4
# After installing spacy, run: python -m spacy download en_core_web_sm

# Regex / fuzzy matching
regex==2024.11.6
rapidfuzz==3.14.5

# indic-nlp-library 0.92 has install issues on Python 3.13 (uses deprecated APIs).
# Excluded — the code already has a regex fallback for Devanagari text.
# If you specifically need Indic language NLP, install it in a Python 3.11 venv instead:
# indic-nlp-library==0.92

# Numerical extraction and validation
num2words==0.5.13

# Structured output / typing
pydantic==2.11.4

# Logging
loguru==0.7.3
pytesseract==0.3.10
easyocr==1.7.2