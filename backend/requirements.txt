# ─────────────────────────────────────────────────────────────────────────────
# TrustNet — backend/requirements.txt
# Unified dependencies for ALL modules (behavior, federated, quantum crypto,
# Benford's, metadata forensics, NLP semantics, GNN).
# Tested: Python 3.13 · Windows 11 · 64-bit
# Install: pip install -r requirements.txt
#
# After install, run ONE extra command for the NLP module:
#   python -m spacy download en_core_web_sm
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn==0.34.3
python-multipart==0.0.20

# ── Data / ML ─────────────────────────────────────────────────────────────────
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2
scipy==1.15.3
matplotlib==3.9.2

# ── Graph (GNN cross-document) ────────────────────────────────────────────────
networkx==3.4.2

# ── PDF ───────────────────────────────────────────────────────────────────────
# PyMuPDF 1.25+ ships pre-built wheels for Python 3.13 on Windows — no compiler needed.
PyMuPDF==1.26.1
pypdf==4.3.1

# ── Office document metadata ──────────────────────────────────────────────────
python-docx==1.1.0
openpyxl==3.1.2
python-pptx==0.6.23

# ── Image / EXIF ──────────────────────────────────────────────────────────────
Pillow==11.1.0
piexif==1.1.3
pyexiftool==0.5.6
# NOTE: also install the ExifTool binary from https://exiftool.org/install.html
# and add it to your Windows PATH. Module degrades gracefully if absent.

# ── File type detection ───────────────────────────────────────────────────────
# python-magic-bin bundles libmagic.dll for Windows — same import name as python-magic.
python-magic-bin==0.4.14

# ── NLP / transformers ────────────────────────────────────────────────────────
transformers==4.53.3
sentence-transformers==5.6.0
spacy==3.8.4
# After install run: python -m spacy download en_core_web_sm

# ── PyTorch (CPU) ─────────────────────────────────────────────────────────────
# CPU-only — works on any laptop, no GPU needed.
# If you have an NVIDIA GPU with CUDA 12.1, replace this line with:
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.7.1

# ── OCR ───────────────────────────────────────────────────────────────────────
pytesseract==0.3.10
# NOTE: Tesseract binary required. Windows installer:
# https://github.com/UB-Mannheim/tesseract/wiki
# After installing, add Tesseract to PATH or set in code:
#   pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
easyocr==1.7.2
opencv-python-headless==4.13.0.92

# ── Text / fuzzy matching ─────────────────────────────────────────────────────
regex==2024.11.6
rapidfuzz==3.14.5
num2words==0.5.13

# ── Quantum crypto ────────────────────────────────────────────────────────────
kyber-py>=1.2.0
cryptography>=42.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.11.4
python-dateutil==2.9.0
loguru==0.7.3